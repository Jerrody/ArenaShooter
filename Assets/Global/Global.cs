namespace Game.Global
{
    public static class Global
    {
        public enum Scenes: uint
        {
            Menu,
            GameLevel,
            TestLevel, // TODO: Remove later.
        }
    }
}